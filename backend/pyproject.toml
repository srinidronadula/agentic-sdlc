[project]
name = "agentic-sdlc-backend"
version = "0.1.0"
description = "Stateful SDLC orchestrator with gates, retries, and an audit log"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
